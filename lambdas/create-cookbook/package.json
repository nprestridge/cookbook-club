{
  "name": "create-cookbook",
  "version": "1.0.0",
  "description": "AWS Lambda to create cookbook",
  "main": "index-create-cookbook.js",
  "scripts": {
    "start": "node index-create-cookbook.js",
    "mock": "ENV=local babel-node mock/run.js",
    "build": "npm run build:clean && npm run build:compile && npm run build:install",
    "build-zip": "npm run build && npm run build:zip",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:compile": "babel src -d dist/src -D && babel index-create-cookbook.js -d dist -D",
    "build:clean": "rm -rf dist && mkdir dist",
    "build:install": "cp package.json dist && find dist -name package.json -execdir npm install --production \\;",
    "build:zip": "cd dist && zip -r index-create-cookbook.zip ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.27.0",
    "babel-runtime": "^6.23.0"
  },
  "devDependencies": {
    "babel-cli": "^6.23.0",
    "babel-core": "^6.23.1",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-preset-latest": "^6.22.0"
  }
}
