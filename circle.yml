
machine:
  node:
    version: 4.2.1

test:
  override:
    - "true"

deployment:
  production:
    branch: deploy
    commands:
      - npm run build
      - aws s3 sync build/ s3://cookbook-club-site/
