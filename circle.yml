
machine:
  node:
    version: 4.2.1

dependencies:
  pre:
    - bash ./circleci/set-configs.sh

test:
  override:
    - "true"

deployment:
  production:
    branch: deploy
    commands:
      - npm run build
      - aws s3 sync build/ s3://cookbook-club-site/
